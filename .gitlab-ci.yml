image: python:3.9

stages:
  - test

before_script:
  # Установка окружения
  - CHCP 65001
  - echo "Install environment"
  - apt-get update -q -y
  - pip3 install -r requirements.txt

job 1:
  stage: test
  script:
    - pytest .\tests\test.py -v
